f you want to stay 100% local and never use an API key, you have to go back to the previous method:

    Use the duckduckgo-search Python library (it scrapes the web from your laptop's IP).

    Manually feed that text into the LLM.

The script you found is much cleaner and more "intelligent," as the model itself decides when it needs to search. If you don't mind the API key, the script you found is the superior way to build a search agent in 2026.